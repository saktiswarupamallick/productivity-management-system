/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: true,
      },
      images: {
        domains: ['llvaevcayjatqqigklxd.supabase.co'],
      },
};

export default nextConfig;
